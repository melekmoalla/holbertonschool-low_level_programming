#include "main.h"

/**
 * print_alphabet_x10 - a function that prints 10 times the alphabet
 * Return: 0
 */

int _islower(int c)
{

    if (c >= 97 && c <= 122)
        return (1);
    else
        return (0);
}