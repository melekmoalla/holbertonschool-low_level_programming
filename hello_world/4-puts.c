#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char text[] = "\"Programing is like building a multilingual puzzle\n";

	puts(text);
	return (0);
}
